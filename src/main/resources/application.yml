spring:
  application:
    name: sentinel-trade

sentinel:
  jwt:
    secret: ${JWT_SECRET:change-me-in-production-min-32-chars}
    expiration-ms: 3600000

management:
  endpoints:
    web:
      exposure:
        include: health
